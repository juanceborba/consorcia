# grafana/datasources/datasource.yml — datasource Prometheus para ConsorcIA
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
